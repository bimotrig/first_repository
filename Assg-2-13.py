print("int('1002',2) error because the 1002 is not a number with base 2, so cannot converted into decimal")
print("int('1008',8) error because the 1008 is not a number with base 8, so cannot converted into decimal")
print("int('AAFG',16) error because the AAFG is not a number with base 16, so cannot converted into decimal")
